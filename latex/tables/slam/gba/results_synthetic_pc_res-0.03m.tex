{\setlength{\tabcolsep}{3pt}
\begin{table}[!t]
\centering
\caption[%
    Pose error of the distance map-based \gls{gba} formulations on a synthetic
    Manhattan-like benchmark]{%
    Pose error of the distance map-based \gls{gba} formulations
    (see~\secref{sec:slam:gba}) on the synthetic Manhattan-like benchmark,
    consisting of 289 $SE(2)$ ground-truth poses arranged in
    a $(2g+1)\times(2g+1)$ grid with $g = 8$,
    placed inside a square scene of half-side $\SI{10}{\metre}$ and observed
    by synthetic scans of a Hokuyo~UST-10LX-like 2D laser scanner sensor
    ($\SI{30}{\metre}$ maximum range, $\SI{0.25}{\degree}$ angular resolution,
    but with a $\SI{360}{\degree}$ field-of-view).
    Pairwise factors connect every pair of variables within a loop-closure
    radius of $r_{\mathrm{loop}} = \SI{1.1}{\metre}$; the first variable is
    fixed and the remaining poses are perturbed by uniform noise of
    $\pm\SI{1.5}{\metre}$ on translation and $\pm\SI{20}{\degree}$ on rotation
    before the optimization stage.
    Two factor formulations are evaluated:
    point-to-point distance map (\ $\mathcal{D}_{\mathrm{p2p}}$,
    see~\secref{sec:slam:gba:p2p}), with representation levels
    $\mathcal{M}_l \in \{\mathcal{M}_0, \mathcal{M}_1\}$, and
    point-to-plane distance map (\ $\mathcal{D}_{\mathrm{p2pl}}$,
    see~\secref{sec:slam:gba:p2pl}), with
    $\mathcal{M}_l \in \{\mathcal{M}_0, \mathcal{M}_2\}$.
    Each formulation
    and representation level is evaluated with (\cmark) and without (\xmark)
    voxelization of the moving point clouds at the distance map resolution
    $\delta = \SI{0.03}{\metre}$.
    Two metrics are reported for each configuration across the noise-free
    case (No Noise) and the case with Gaussian range noise of standard
    deviation $\sigma = \SI{0.03}{\metre}$:
    $\varepsilon_{\mathbf{t}}$ (\si{\metre}), the translational \gls{rmse}
    of \eqref{eq:slam:results:gba:metrics:trans}, and $\varepsilon_{\theta}$
    (\si{\degree}), the rotational \gls{rmse} of
    \eqref{eq:slam:results:gba:metrics:rot}, both aggregated over the 288
    non-fixed poses of the grid. Bold entries mark the best result
    per metric within each noise condition.%
}
\label{tab:slam:results:gba}
\begin{tabular}{
    l     % method family  (multirow label)
    l     % level          (M_0 / M_1 / M_2)
    c |   % variant flags  (V - voxelization)
    % 4 scene groups
    S[table-format=1.3] S[table-format=1.2] |   % no noise
    S[table-format=1.3] S[table-format=1.2]     % with noise (std = 0.03m)
}
\hline
\multicolumn{3}{c|}{} &
\multicolumn{2}{c|}{\textbf{No Noise}} &
\multicolumn{2}{c}{\textbf{$\sigma = \SI{0.03}{\metre}$}}\\
% \multicolumn{3}{c|}{} &
% \multicolumn{2}{c|}{} &
% \multicolumn{2}{c}{()}\\
\multicolumn{3}{c|}{} &
\multicolumn{1}{c}{$\varepsilon_t$} &
\multicolumn{1}{c|}{$\varepsilon_\theta$} &
\multicolumn{1}{c}{$\varepsilon_t$} &
\multicolumn{1}{c}{$\varepsilon_\theta$}\\
\multicolumn{1}{l}{\textbf{Meth.}} &
\multicolumn{1}{c}{$\mathbf{\mathcal{M}_l}$} &
\multicolumn{1}{c|}{\textbf{V?}} &
\multicolumn{1}{c}{(m)} &
\multicolumn{1}{c|}{(\textdegree{})} &
\multicolumn{1}{c}{(m)} &
\multicolumn{1}{c}{(\textdegree{})}\\
\hline

% ==============================================================================
% DISTANCE MAP POINT-TO-POINT GBA ERROR FORMULATION
% ==============================================================================

\multirow{4}{*}{$\mathcal{D}_\mathrm{p2p}$} & \multirow{2}{*}{$\mathcal{M}_0$}
  & \xmark &            % wo/voxelization
0.267 & 2.05 &          % no noise
0.187 & 1.51 \\         % with noise
& & \cmark &            % w/voxelization
0.013 & 0.06 &          % no noise
0.009 & 0.06 \\         % with noise
\cline{2-7}
& \multirow{2}{*}{$\mathcal{M}_1$}
  & \xmark &            % wo/voxelization
0.042 & 0.04 &          % no noise
\B 0.003 & \B 0.02 \\   % with noise
& & \cmark &            % w/voxelization
0.046 & 0.04 &          % no noise
0.004 & \B 0.02 \\      % with noise

\hline

% ==============================================================================
% DISTANCE MAP POINT-TO-PLANE GBA ERROR FORMULATION
% ==============================================================================

\multirow{4}{*}{$\mathcal{D}_\mathrm{p2pl}$} & \multirow{2}{*}{$\mathcal{M}_0$}
  & \xmark &            % wo/voxelization
\B 0.000 & \B 0.00 &    % no noise
0.021 & 0.17 \\         % with noise
& & \cmark &            % w/voxelization
\B 0.000 & \B 0.00 &    % no noise
0.009 & 0.05 \\         % with noise
\cline{2-7}
& \multirow{2}{*}{$\mathcal{M}_2$}
  & \xmark &            % wo/voxelization
0.027 & 0.12 &          % no noise
0.004 & 0.03 \\         % with noise
& & \cmark &            % w/voxelization
0.017 & 0.08 &          % no noise
0.005 & 0.03 \\         % with noise

\hline
\end{tabular}
\end{table}}
